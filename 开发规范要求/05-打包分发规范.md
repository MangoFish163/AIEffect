# AIEffect - 打包分发规范文档

## 1. 概述

### 1.1 文档目的

本文档定义 AIEffect 微服务架构应用的打包、部署和分发规范，确保应用能够基于 Docker 容器化技术稳定部署和运行。

### 1.2 部署架构

- **部署方式**：Docker Compose 容器编排
- **目标平台**：支持 Docker 的所有平台（Windows/macOS/Linux）
- **服务架构**：微服务架构（API Gateway + WebSocket + Log Service + Frontend）

---

## 2. 服务端口配置

### 2.1 端口分配（AIEffect 专用区间 8500-8505）

| 端口 | 服务            | 配置项             | 说明             | 协议      |
| ---- | --------------- | ------------------ | ---------------- | --------- |
| 8500 | **Frontend**    | `FRONTEND_PORT`    | 前端服务(Vite)   | HTTP      |
| 8501 | **API Gateway** | `API_GATEWAY_PORT` | FastAPI 主服务   | HTTP/REST |
| 8502 | **WebSocket**   | `WEBSOCKET_PORT`   | WebSocket 服务   | WebSocket |
| 8503 | **（保留）**    | -                  | 字幕服务（未来） | -         |
| 8504 | **（保留）**    | -                  | TTS 服务（未来） | -         |
| 8505 | **Log Service** | `LOG_SERVICE_PORT` | 日志服务         | HTTP/REST |

### 2.2 端口冲突处理策略

```python
# shared/python/shared_core/port_manager.py
import socket
from typing import Optional

class PortManager:
    """端口管理器，处理端口分配和冲突"""

    DEFAULT_PORTS = {
        'frontend': 8500,
        'api_gateway': 8501,
        'websocket': 8502,
        'subtitle': 8503,
        'tts': 8504,
        'log_service': 8505,
    }

    PORT_RANGE = (8500, 8505)

    @classmethod
    def is_port_available(cls, port: int, host: str = "0.0.0.0") -> bool:
        """检查端口是否可用"""
        try:
            with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
                s.bind((host, port))
                return True
        except OSError:
            return False

    @classmethod
    def find_available_port(cls, preferred_port: int) -> int:
        """查找可用端口，优先使用指定端口"""
        if cls.is_port_available(preferred_port):
            return preferred_port

        # 在范围内查找下一个可用端口
        for port in range(preferred_port + 1, cls.PORT_RANGE[1] + 1):
            if cls.is_port_available(port):
                return port

        raise RuntimeError(f"无法在范围 {cls.PORT_RANGE} 内找到可用端口")

    @classmethod
    def get_service_port(cls, service_name: str, config: Optional[dict] = None) -> int:
        """获取服务端口，支持配置覆盖"""
        preferred = config.get(f'{service_name}_port') if config else None
        preferred = preferred or cls.DEFAULT_PORTS.get(service_name, 8500)
        return cls.find_available_port(preferred)
```

### 2.3 端口配置示例

```yaml
# config/ports.yaml
ports:
  frontend:
    default: 8500
    description: "前端服务端口(Vite)"
    env_var: "FRONTEND_PORT"

  api_gateway:
    default: 8501
    description: "API Gateway 服务端口"
    env_var: "API_GATEWAY_PORT"

  websocket:
    default: 8502
    description: "WebSocket 服务端口"
    env_var: "WEBSOCKET_PORT"

  subtitle:
    default: 8503
    description: "字幕服务端口（预留）"
    env_var: "SUBTITLE_PORT"

  tts:
    default: 8504
    description: "TTS 服务端口（预留）"
    env_var: "TTS_PORT"

  log_service:
    default: 8505
    description: "日志服务端口"
    env_var: "LOG_SERVICE_PORT"

# 端口冲突处理
conflict_resolution:
  auto_increment: false
  max_retry: 0
  notify_user: true
```

---

## 3. Docker 镜像构建

### 3.1 Python 服务镜像（API Gateway / Log Service / WebSocket）

#### Dockerfile 模板

```dockerfile
# services/api_gateway/Dockerfile
FROM python:3.11-slim as builder

WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    gcc \
    && rm -rf /var/lib/apt/lists/*

# 安装 Poetry
RUN pip install poetry

# 复制依赖文件
COPY pyproject.toml poetry.lock* ./

# 安装依赖
RUN poetry config virtualenvs.create false \
    && poetry install --no-dev --no-interaction --no-ansi

# 生产镜像
FROM python:3.11-slim

WORKDIR /app

# 创建非 root 用户
RUN groupadd -r aieffect && useradd -r -g aieffect aieffect

# 复制依赖
COPY --from=builder /usr/local/lib/python3.11/site-packages /usr/local/lib/python3.11/site-packages
COPY --from=builder /usr/local/bin /usr/local/bin

# 复制应用代码
COPY src/ ./src/

# 创建数据目录
RUN mkdir -p /app/data && chown -R aieffect:aieffect /app

# 切换到非 root 用户
USER aieffect

# 暴露端口
EXPOSE 8501

# 健康检查
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD python -c "import urllib.request; urllib.request.urlopen('http://localhost:8501/health')" || exit 1

# 启动命令
CMD ["python", "-m", "uvicorn", "src.api_gateway.main:app", "--host", "0.0.0.0", "--port", "8501"]
```

#### 多阶段构建优势

| 特性         | 说明                            |
| ------------ | ------------------------------- |
| 镜像体积     | 减少约 60%（从 ~1GB 到 ~400MB） |
| 安全性       | 移除编译工具，减少攻击面        |
| 构建速度     | 依赖层缓存，增量构建更快        |
| 非 root 用户 | 提升容器运行安全性              |

### 3.2 前端镜像

```dockerfile
# frontend/Dockerfile
# 构建阶段
FROM node:20-alpine as builder

WORKDIR /app

# 复制依赖文件
COPY package*.json ./

# 安装依赖
RUN npm ci

# 复制源码
COPY . .

# 构建
RUN npm run build

# 生产阶段
FROM nginx:alpine

# 复制构建产物
COPY --from=builder /app/dist /usr/share/nginx/html

# 复制 nginx 配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 暴露端口
EXPOSE 80

# 健康检查
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD wget --quiet --tries=1 --spider http://localhost/ || exit 1

CMD ["nginx", "-g", "daemon off;"]
```

### 3.3 Nginx 配置

```nginx
# frontend/nginx.conf
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/xml;

    # 前端路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 代理
    location /api/ {
        proxy_pass http://api_gateway:8501/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # WebSocket 代理
    location /ws/ {
        proxy_pass http://websocket:8502/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 86400;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

---

## 4. Docker Compose 部署配置

### 4.1 生产环境配置

```yaml
# docker-compose.yml
version: "3.8"

services:
  # 前端服务
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "${FRONTEND_PORT:-8500}:80"
    environment:
      - NGINX_ENVSUBST_OUTPUT_DIR=/etc/nginx
    depends_on:
      - api_gateway
    restart: unless-stopped
    networks:
      - aieffect-network

  # API 网关服务
  api_gateway:
    build:
      context: ./services/api_gateway
      dockerfile: Dockerfile
    ports:
      - "${API_GATEWAY_PORT:-8501}:8501"
    environment:
      - API_GATEWAY_PORT=8501
      - LOG_SERVICE_URL=http://log_service:8505
      - WEBSOCKET_URL=ws://websocket:8502
      - OLLAMA_BASE_URL=${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - DATA_DIR=/app/data
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    depends_on:
      - log_service
    restart: unless-stopped
    networks:
      - aieffect-network
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8501/health')",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # WebSocket 服务
  websocket:
    build:
      context: ./services/websocket
      dockerfile: Dockerfile
    ports:
      - "${WEBSOCKET_PORT:-8502}:8502"
    environment:
      - WEBSOCKET_PORT=8502
      - API_GATEWAY_URL=http://api_gateway:8501
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    depends_on:
      - api_gateway
    restart: unless-stopped
    networks:
      - aieffect-network
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8502/health')",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # 日志服务
  log_service:
    build:
      context: ./services/log
      dockerfile: Dockerfile
    ports:
      - "${LOG_SERVICE_PORT:-8505}:8505"
    environment:
      - LOG_SERVICE_PORT=8505
      - DATA_DIR=/app/data
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    volumes:
      - ./data/logs:/app/data
    restart: unless-stopped
    networks:
      - aieffect-network
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8505/health')",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

networks:
  aieffect-network:
    driver: bridge

volumes:
  data:
    driver: local
  logs:
    driver: local
```

### 4.2 开发环境配置

```yaml
# docker-compose.dev.yml
version: "3.8"

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "${FRONTEND_PORT:-8500}:8500"
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/public:/app/public
    environment:
      - VITE_API_URL=http://localhost:8501
      - VITE_WS_URL=ws://localhost:8502
    command: npm run dev -- --host --port 8500

  api_gateway:
    build:
      context: ./services/api_gateway
      dockerfile: Dockerfile
    ports:
      - "${API_GATEWAY_PORT:-8501}:8501"
    volumes:
      - ./services/api_gateway/src:/app/src
      - ./shared/python:/app/shared
      - ./data:/app/data
    environment:
      - LOG_SERVICE_URL=http://log_service:8505
    command: uvicorn src.api_gateway.main:app --host 0.0.0.0 --port 8501 --reload

  log_service:
    build:
      context: ./services/log
      dockerfile: Dockerfile
    ports:
      - "${LOG_SERVICE_PORT:-8505}:8505"
    volumes:
      - ./services/log/src:/app/src
      - ./data/logs:/app/data
    command: uvicorn src.main:app --host 0.0.0.0 --port 8505 --reload

  websocket:
    build:
      context: ./services/websocket
      dockerfile: Dockerfile
    ports:
      - "${WEBSOCKET_PORT:-8502}:8502"
    volumes:
      - ./services/websocket/src:/app/src
    environment:
      - API_GATEWAY_URL=http://api_gateway:8501
    command: uvicorn src.websocket_service.main:app --host 0.0.0.0 --port 8502 --reload
```

### 4.3 环境变量配置

```bash
# .env.example
# 服务端口 (AIEffect 专用区间 8500-8505)
FRONTEND_PORT=8500
API_GATEWAY_PORT=8501
WEBSOCKET_PORT=8502
LOG_SERVICE_PORT=8505

# API 配置
OLLAMA_BASE_URL=http://host.docker.internal:11434
OPENAI_API_KEY=your-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1

# 应用配置
DATA_DIR=./data
LOG_LEVEL=INFO

# Docker 配置
COMPOSE_PROJECT_NAME=aieffect
COMPOSE_FILE=docker-compose.yml
```

---

## 5. 部署流程

### 5.1 首次部署

```bash
#!/bin/bash
# scripts/deploy.sh

set -e

echo "🚀 AIEffect 部署脚本"

# 1. 检查环境
echo "📋 检查环境..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装"
    exit 1
fi

if ! command -v docker-compose &> /dev/null; then
    echo "❌ Docker Compose 未安装"
    exit 1
fi

# 2. 创建数据目录
echo "📁 创建数据目录..."
mkdir -p data/logs
mkdir -p logs

# 3. 配置环境变量
echo "⚙️ 配置环境变量..."
if [ ! -f .env ]; then
    cp .env.example .env
    echo "⚠️ 请编辑 .env 文件配置必要参数"
fi

# 4. 拉取/构建镜像
echo "🐳 构建镜像..."
docker-compose build

# 5. 启动服务
echo "▶️ 启动服务..."
docker-compose up -d

# 6. 等待服务就绪
echo "⏳ 等待服务就绪..."
sleep 10

# 7. 健康检查
echo "🏥 健康检查..."
docker-compose ps

# 8. 显示访问信息
echo ""
echo "✅ 部署完成！"
echo ""
echo "📱 前端访问: http://localhost:8500"
echo "🔌 API 访问: http://localhost:8501"
echo "🔔 WebSocket: ws://localhost:8502"
echo "📊 日志服务: http://localhost:8505"
echo ""
echo "📋 常用命令:"
echo "  查看日志: docker-compose logs -f"
echo "  停止服务: docker-compose down"
echo "  重启服务: docker-compose restart"
```

### 5.2 更新部署

```bash
#!/bin/bash
# scripts/update.sh

set -e

echo "🔄 AIEffect 更新脚本"

# 1. 拉取最新代码
echo "📥 拉取最新代码..."
git pull origin main

# 2. 重新构建镜像
echo "🐳 重新构建镜像..."
docker-compose build --no-cache

# 3. 滚动更新
echo "🔄 滚动更新服务..."
docker-compose up -d --remove-orphans

# 4. 清理旧镜像
echo "🧹 清理旧镜像..."
docker image prune -f

echo "✅ 更新完成！"
```

### 5.3 备份与恢复

```bash
#!/bin/bash
# scripts/backup.sh

BACKUP_DIR="./backups"
DATE=$(date +%Y%m%d_%H%M%S)

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据
echo "💾 备份数据..."
tar -czf "$BACKUP_DIR/aieffect_data_$DATE.tar.gz" data/

# 备份配置
cp .env "$BACKUP_DIR/aieffect_env_$DATE.bak"

echo "✅ 备份完成: $BACKUP_DIR/aieffect_data_$DATE.tar.gz"
```

```bash
#!/bin/bash
# scripts/restore.sh

BACKUP_FILE=$1

if [ -z "$BACKUP_FILE" ]; then
    echo "用法: ./restore.sh <备份文件>"
    exit 1
fi

# 停止服务
docker-compose down

# 恢复数据
echo "📥 恢复数据..."
tar -xzf $BACKUP_FILE

# 启动服务
docker-compose up -d

echo "✅ 恢复完成！"
```

---

## 6. 监控与日志

### 6.1 日志收集

```yaml
# docker-compose.logging.yml
version: "3.8"

services:
  # 使用 Fluentd 收集日志
  fluentd:
    image: fluent/fluentd:v1.16
    volumes:
      - ./fluentd/conf:/fluentd/etc
    ports:
      - "24224:24224"
      - "24224:24224/udp"
    networks:
      - aieffect-network

  # 可选：使用 ELK 栈
  elasticsearch:
    image: elasticsearch:8.11.0
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
    volumes:
      - elasticsearch-data:/usr/share/elasticsearch/data
    ports:
      - "9200:9200"
    networks:
      - aieffect-network

  kibana:
    image: kibana:8.11.0
    ports:
      - "5601:5601"
    depends_on:
      - elasticsearch
    networks:
      - aieffect-network

volumes:
  elasticsearch-data:
```

### 6.2 监控配置

```yaml
# docker-compose.monitoring.yml
version: "3.8"

services:
  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus:/etc/prometheus
      - prometheus-data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
    networks:
      - aieffect-network

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/dashboards:/etc/grafana/provisioning/dashboards
    depends_on:
      - prometheus
    networks:
      - aieffect-network

volumes:
  prometheus-data:
  grafana-data:
```

---

## 7. 性能优化

### 7.1 镜像优化

| 优化项        | 方法                        | 效果           |
| ------------- | --------------------------- | -------------- |
| 多阶段构建    | 使用 builder + runtime 阶段 | 减少 60% 体积  |
| 基础镜像      | 使用 python:3.11-slim       | 减少 50% 体积  |
| 依赖缓存      | 先复制依赖文件再安装        | 加速构建       |
| .dockerignore | 排除不需要的文件            | 减少构建上下文 |

### 7.2 运行时优化

```yaml
# docker-compose.yml 优化配置
services:
  api_gateway:
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
        reservations:
          cpus: "0.5"
          memory: 256M
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
```

### 7.3 网络优化

- 使用自定义 bridge 网络
- 服务间通过服务名通信
- 启用连接复用

---

## 8. 安全规范

### 8.1 镜像安全

- 使用官方基础镜像
- 定期更新基础镜像
- 扫描镜像漏洞
- 最小权限原则（非 root 用户）

```bash
# 镜像安全扫描
docker run --rm -v /var/run/docker.sock:/var/run/docker.sock \
    aquasec/trivy image aieffect-api-gateway:latest
```

### 8.2 运行时安全

- 限制容器资源
- 只暴露必要端口
- 使用只读文件系统（可选）
- 禁用特权模式

### 8.3 网络安全

- 使用 TLS/SSL（生产环境）
- 配置防火墙规则
- 使用 VPN 或私有网络

---

## 9. 常见问题排查

### 9.1 端口冲突

```bash
# 检查端口占用
sudo lsof -i :8501

# 修改端口后重启
export API_GATEWAY_PORT=8510
docker-compose up -d
```

### 9.2 服务启动失败

```bash
# 查看详细日志
docker-compose logs -f api_gateway

# 进入容器调试
docker-compose exec api_gateway /bin/sh
```

### 9.3 数据持久化

```bash
# 确保数据目录权限正确
sudo chown -R 1000:1000 ./data

# 备份数据
docker-compose exec api_gateway tar -czf /tmp/backup.tar.gz /app/data
docker cp aieffect_api_gateway_1:/tmp/backup.tar.gz ./backup.tar.gz
```

---

## 10. 更新记录

| 版本   | 日期       | 更新内容                        |
| ------ | ---------- | ------------------------------- |
| v1.0.0 | 2024-XX-XX | 初始版本，Electron 桌面应用打包 |
| v2.0.0 | 2024-XX-XX | 重构为 Docker 容器化部署规范    |
